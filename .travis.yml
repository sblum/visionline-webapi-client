language: php
php:
  - '7.2'
 
before_install:
  - rm -f ~/.phpenv/versions/$(phpenv version-name)/etc/conf.d/xdebug.ini
  
install:
  - composer install
  
script:
  - vendor/bin/php-cs-fixer --dry-run --diff -v fix 
